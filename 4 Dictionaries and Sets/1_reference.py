"""   
input() --- easy

str.isDigit() --- true if all str are digits (0-9) 

=================================

initiate dictionary --- d = {'a':1, 'b':2} 

d['a'] --- access value

d.get('a', optional) --- get value like d['a'] except if value not in dictionary, it return either 'None' or a second optional value

d.items() --- returns iterator

=================================

set --- {1,2,3} --- stores unique values

set.add(10) --- {1,2,3,10}

set.update([10,20,30])   --- adds the elements of one or more iterables to a set

"""