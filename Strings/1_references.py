#Useful string functions

"""
[in] -- 'a' in 'abcd' -> true

slices --- 'abcdef'[1(start):7(end):2(steps)]

str.split -- splits a string into an array

str.join --- example: '*'.join(['ab','cd']) -> ab*cd

list.append -- add element to list

sorted(list) ---> sorts a list depending on the input

iterating over a file: for line in open(filename)
"""